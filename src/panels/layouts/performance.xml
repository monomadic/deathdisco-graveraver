<?xml version="1.0" encoding="UTF-8"?>
<module>
  <!-- PERFORMANCE
    Assumes the user will be heavily slanted to controller use,
    and the VirtualDJ screen mostly for visual cues and feedback over interaction.
  -->
  <group name="PERFORMANCE" condition="var_equal '@$skin_mode' 1">
    <!-- @$layout_4deck selects deck density within Performance: 0 = two-deck / swap, 1 = full four-deck -->
    <!-- Main decks -->
    <group name="Extended" condition="var_equal '@$performance_layout' 0">
      <panel class="performance_decks_above" condition="var_equal '@$hide_main_waveforms' 0 ? var_equal '@$waveform_position' 0" visibility="var '@$browser_zoom_mode' 1 ? browser_zoom ? false : browser_isactive ? false : true : browser_zoom ? false : true"/>
      <panel class="performance_decks_below" condition="var_equal '@$hide_main_waveforms' 0 ? var_equal '@$waveform_position' 1" visibility="var '@$browser_zoom_mode' 1 ? browser_zoom ? false : browser_isactive ? false : true : browser_zoom ? false : true"/>
      <panel class="performance_decks_no_waves" condition="var_equal '@$hide_main_waveforms' 1" visibility="var '@$browser_zoom_mode' 1 ? browser_zoom ? false : browser_isactive ? false : true : browser_zoom ? false : true"/>
    </group>
    <group name="Vertical" condition="var_equal '@$performance_layout' 1">
      <panel class="performance_decks_vertical" visibility="var '@$browser_zoom_mode' 1 ? browser_zoom ? false : browser_isactive ? false : true : browser_zoom ? false : true"/>
    </group>
    <group name="Horizontal" condition="var_equal '@$performance_layout' 2">
      <panel class="performance_decks_center" visibility="var '@$browser_zoom_mode' 1 ? browser_zoom ? false : browser_isactive ? false : true : browser_zoom ? false : true"/>
    </group>
    <!-- browser zoom & auto-switch -->
    <panel class="performancemini_decks_above" condition="var_equal '@$hide_zoom_waveforms' 0 ? var_equal '@$waveform_position' 0" visibility="var '@$browser_zoom_mode' 1 ? browser_zoom ? true : browser_isactive ? true : false : browser_zoom ? true : false"/>
    <panel class="performancemini_decks_below" condition="var_equal '@$hide_zoom_waveforms' 0 ? var_equal '@$waveform_position' 1" visibility="var '@$browser_zoom_mode' 1 ? browser_zoom ? true : browser_isactive ? true : false : browser_zoom ? true : false"/>
    <panel class="performancemini_decks_no_waves" condition="var_equal '@$hide_zoom_waveforms' 1" visibility="var '@$browser_zoom_mode' 1 ? browser_zoom ? true : browser_isactive ? true : false : browser_zoom ? true : false"/>
  </group>
</module>
